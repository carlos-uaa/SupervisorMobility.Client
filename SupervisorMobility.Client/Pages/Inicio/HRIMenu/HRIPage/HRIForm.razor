@page "/hri/form"
@using SupervisorMobility.Client.Data.Entites.Dtos.HRIDtos
@using SupervisorMobility.Client.Data.Entities.Dtos.HRIDtos.HRIImagesDtos
@using SupervisorMobility.Client.Services.HRIServices;
@inject IHRIService HRIService;

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4 mb-10">

    <div class="d-flex justify-space-between align-center mb-8 px-2">
        <div>
            <MudText Typo="Typo.h4" Color="Color.Primary" Style="font-weight: 700;">
                Creación de Hoja de Revisión Inicial
            </MudText>
            <MudText Typo="Typo.subtitle1" Class="mud-text-secondary mt-1">
                Configuración inicial de parámetros y puntos de revisión
            </MudText>
        </div>
    </div>

    <MudGrid Spacing="4">
        <MudItem xs="12">
            <MudPaper Elevation="0" Outlined="true" Class="rounded-xl pa-4" Style="border-left: 5px solid var(--mud-palette-info)">
                <div class="d-flex align-center mb-4">
                    <MudAvatar Color="Color.Info" Variant="Variant.Filled" Size="Size.Medium" Rounded="true" Class="mr-3">
                        <MudIcon Icon="@Icons.Material.Filled.DisplaySettings" Size="Size.Small" />
                    </MudAvatar>
                    <MudText Typo="Typo.h6" Style="font-weight: 600;">I. Información General y Evidencia</MudText>
                </div>

                <div class="mb-6">
                    <MudText Typo="Typo.subtitle2" Class="mb-2" Color="Color.Tertiary" Style="font-weight: 600; text-transform: uppercase;">
                        LAY OUT:
                    </MudText>

                    @if (UploadedImages == null || !UploadedImages.Any())
                    {
                        <MudPaper Elevation="0" Outlined="true" Class="d-flex flex-column align-center justify-center py-10 rounded-lg bg-light-grey" Style="border-style: dashed; border-color: var(--mud-palette-lines-default);">
                            <MudIcon Icon="@Icons.Material.Filled.ImageNotSupported" Size="Size.Large" Color="Color.Default" Class="mb-3 opacity-50" />
                            <MudText Typo="Typo.body1" Color="Color.Default" Class="mb-4" Style="font-weight: 500;">
                                No se han cargado imágenes de evidencia
                            </MudText>
            
                            <MudFileUpload T="IReadOnlyList<IBrowserFile>" 
                                           OnFilesChanged="UploadFiles" 
                                           AppendMultipleFiles="true"
                                           Accept=".jpg, .jpeg, .png"
                                           Hidden="true"
                                           InputClass="absolute mud-width-full mud-height-full overflow-hidden z-20 opacity-0"
                                           InputStyle="cursor: pointer;">
                                <ButtonTemplate Context="fileContext">
                                    <MudButton HtmlTag="label"
                                               Variant="Variant.Outlined"
                                               Color="Color.Primary"
                                               for="@fileContext"
                                               StartIcon="@Icons.Material.Filled.CloudUpload">
                                        Cargar Imágenes
                                    </MudButton>
                                </ButtonTemplate>
                            </MudFileUpload>
                        </MudPaper>
                    }
                    else
                    {
                        <div class="d-flex gap-3 overflow-x-auto py-2 px-1 custom-scrollbar" style="width: 100%; min-height: 160px; align-items: center;">
            
                            @foreach (var entry in UploadedImages)
                            {
                                <div class="position-relative d-inline-block flex-shrink-0 rounded-lg overflow-hidden mud-elevation-2" 
                                     style="height: 150px; border: 1px solid var(--mud-palette-lines-default);">
        
                                    <MudImage Src="@entry.Value.Uri" 
                                              Height="150" 
                                              ObjectFit="ObjectFit.Cover" 
                                              Style="width: auto; display: block;" 
                                              Alt="@entry.Key" />
        
                                    <div class="position-absolute top-0 right-0 ma-1" style="z-index: 10;">
                                        <MudIconButton Icon="@Icons.Material.Filled.Delete" 
                                                       Color="Color.Error" 
                                                       Variant="Variant.Filled" 
                                                       Size="Size.Small" 
                                                       Class="pa-1"
                                                       Style="background-color: rgba(255, 255, 255, 0.9); backdrop-filter: blur(2px); border-radius: 4px;"
                                                       OnClick="() => RemoveImage(entry.Key)" />
                                    </div>
                                </div>
                            }

                            <MudFileUpload T="IReadOnlyList<IBrowserFile>" Accept=".jpg, .jpeg, .png" Multiple="true" OnFilesChanged="UploadFiles" Class="flex-shrink-0 mt-0">
                                <ButtonTemplate Context="fileContext2">
                                    <MudButton HtmlTag="label"
                                               Variant="Variant.Outlined" 
                                               Color="Color.Primary" 
                                               Style="height: 150px; border-style: dashed; border-width: 2px;" 
                                               for="@fileContext2"
                                               StartIcon="@Icons.Material.Filled.AddPhotoAlternate">
                                        Agregar
                                    </MudButton>
                                </ButtonTemplate>
                            </MudFileUpload>
                        </div>
                    }
                </div>

                <MudDivider Class="mb-6" />

                <MudGrid Spacing="3">
                    <MudItem xs="12" md="4">
                        <MudTextField T="string" Label="Nombre de Item" Variant="Variant.Outlined" Margin="Margin.Dense" Placeholder="Ej. Montacargas" />
                    </MudItem>
                    <MudItem xs="12" md="4">
                        <MudTextField T="string" Label="Nombre de Línea" Variant="Variant.Outlined" Margin="Margin.Dense" Placeholder="Ej. Embarques" />
                    </MudItem>
                    <MudItem xs="12" md="4">
                        <MudTextField Label="Número de Control" ReadOnly="true" Variant="Variant.Outlined" Margin="Margin.Dense" Value="@("MT-28")" />
                    </MudItem>

                    <MudItem xs="12" md="3">
                        <MudSelect T="int" Label="Dock Asignado" Variant="Variant.Outlined" Margin="Margin.Dense" AnchorOrigin="Origin.BottomCenter">
                            <MudSelectItem Value="0" Disabled>Select Dock...</MudSelectItem>
                            <MudSelectItem Value="1">Dock A - Norte</MudSelectItem>
                            <MudSelectItem Value="2">Dock B - Sur</MudSelectItem>
                        </MudSelect>
                    </MudItem>
                    <MudItem xs="12" md="3">
                        <MudSelect T="int" Label="Departamento" Variant="Variant.Outlined" Margin="Margin.Dense" AnchorOrigin="Origin.BottomCenter">
                            <MudSelectItem Value="0" Disabled>Select Department...</MudSelectItem>
                            <MudSelectItem Value="1">Calidad</MudSelectItem>
                            <MudSelectItem Value="2">Producción</MudSelectItem>
                        </MudSelect>
                    </MudItem>
                    <MudItem xs="12" md="3">
                        <MudSelect T="int" Label="Supervisor" Variant="Variant.Outlined" Margin="Margin.Dense" AnchorOrigin="Origin.BottomCenter">
                            <MudSelectItem Value="0" Disabled>Select Supervisor...</MudSelectItem>
                            <MudSelectItem Value="1">Juan Pérez</MudSelectItem>
                            <MudSelectItem Value="2">María García</MudSelectItem>
                        </MudSelect>
                    </MudItem>
                    <MudItem xs="12" md="3">
                        <MudNumericField @bind-Value="NumeroCiclos" Label="Número de ciclos del documento" Variant="Variant.Outlined" Margin="Margin.Dense" Min="1" Max="10" />
                    </MudItem>
                </MudGrid>
            </MudPaper>
        </MudItem>

        <MudItem xs="12">
            <MudPaper Elevation="0" Outlined="true" Class="rounded-xl pa-4" Style="border-left: 5px solid var(--mud-palette-warning)">
                <div class="d-flex align-center mb-4">
                    <MudAvatar Color="Color.Warning" Variant="Variant.Filled" Size="Size.Medium" Rounded="true" Class="mr-3">
                        <MudIcon Icon="@Icons.Material.Filled.Checklist" Size="Size.Small" />
                    </MudAvatar>
                    <MudText Typo="Typo.h6" Style="font-weight: 600;">II. Puntos de Revisión</MudText>
                    <MudSpacer />
                    <MudButton StartIcon="@Icons.Material.Filled.Add" Variant="Variant.Filled" Color="Color.Success" Size="Size.Small">
                        Agregar Punto
                    </MudButton>
                </div>

                <MudSimpleTable Dense="true" Hover="true" Elevation="0" Class="mud-width-full border-all border-solid mud-border-lines-default rounded-lg overflow-hidden">
                    <thead>
                        <tr class="bg-light-grey">
                            <th style="width: 60px; text-align:center;">#</th>
                            <th style="min-width: 250px;">Puntos de revisión</th>
                            <th style="width: 200px;">Método de Revisión</th>
                            <th style="min-width: 200px;">Criterio</th>
                            <th style="width: 150px;">Frecuencia</th>
                            <th style="width: 60px;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="text-center">
                                <MudChip Size="Size.Small" Variant="Variant.Text" Color="Color.Default">1</MudChip>
                            </td>
                            <td>
                                <MudTextField T="string" Variant="Variant.Outlined" Margin="Margin.Dense" Placeholder="Descripción del punto..." Class="bg-white" />
                            </td>
                            <td>
                                <MudSelect T="int" Variant="Variant.Outlined" Margin="Margin.Dense" Dense="true" Class="bg-white">
                                    <MudSelectItem Value="0" Disabled>Select...</MudSelectItem>
                                    <MudSelectItem Value="1">Visual</MudSelectItem>
                                    <MudSelectItem Value="2">Medición</MudSelectItem>
                                    <MudSelectItem Value="3">Funcional</MudSelectItem>
                                </MudSelect>
                            </td>
                            <td>
                                <MudTextField T="string" Variant="Variant.Outlined" Margin="Margin.Dense" Placeholder="Ej. Libre de rebabas" Class="bg-white" />
                            </td>
                            <td>
                                <MudSelect T="int" Variant="Variant.Outlined" Margin="Margin.Dense" Dense="true" Class="bg-white">
                                    <MudSelectItem Value="0" Disabled>Select...</MudSelectItem>
                                    <MudSelectItem Value="1">Diario</MudSelectItem>
                                    <MudSelectItem Value="1">Por Turno</MudSelectItem>
                                </MudSelect>
                            </td>
                            <td>
                                <MudIconButton Icon="@Icons.Material.Filled.DeleteOutline" Color="Color.Error" Size="Size.Small" />
                            </td>
                        </tr>
                    </tbody>
                </MudSimpleTable>
            </MudPaper>
        </MudItem>

        <MudItem xs="12">
            <MudPaper Elevation="0" Outlined="true" Class="rounded-xl pa-4" Style="border-left: 5px solid var(--mud-palette-success)">
                <div class="d-flex align-center mb-4">
                    <MudAvatar Color="Color.Success" Variant="Variant.Filled" Size="Size.Medium" Rounded="true" Class="mr-3">
                        <MudIcon Icon="@Icons.Material.Filled.AssignmentInd" Size="Size.Small" />
                    </MudAvatar>
                    <MudText Typo="Typo.h6" Style="font-weight: 600;">III. Asignación de Personal</MudText>
                </div>

                <MudGrid Spacing="3" Class="mb-6">
                    @for (int i = 1; i <= NumeroCiclos; i++)
                    {
                        int currentCycle = i;
                        <MudItem xs="12" sm="6" md="4">
                            <MudPaper Elevation="0" Outlined="true" Class="pa-4 bg-light-grey rounded-lg" Style="border-style: dashed; border-color: var(--mud-palette-lines-default);">
                                <div class="d-flex align-center mb-3">
                                    <MudIcon Icon="@Icons.Material.Filled.Loop" Size="Size.Small" Color="Color.Secondary" Class="mr-2"/>
                                    <MudText Typo="Typo.subtitle2" Color="Color.Primary" Style="font-weight: 600;">Ciclo @currentCycle</MudText>
                                </div>
                                
                                <MudSelect T="int" Label="Operador" Variant="Variant.Outlined" Margin="Margin.Dense" Dense="true" Class="mb-3 bg-white">
                                    <MudSelectItem Value="0" Disabled>Select Operator...</MudSelectItem>
                                    <MudSelectItem Value="1">Operador A</MudSelectItem>
                                    <MudSelectItem Value="2">Operador B</MudSelectItem>
                                </MudSelect>
                                
                                <MudSelect T="int" Label="Supervisor" Variant="Variant.Outlined" Margin="Margin.Dense" Dense="true" Class="bg-white">
                                    <MudSelectItem Value="0" Disabled>Select Supervisor...</MudSelectItem>
                                    <MudSelectItem Value="1">Supervisor A</MudSelectItem>
                                    <MudSelectItem Value="2">Supervisor B</MudSelectItem>
                                </MudSelect>
                            </MudPaper>
                        </MudItem>
                    }
                </MudGrid>

                <MudDivider Class="mb-6" />

                <MudGrid>
                    <MudItem xs="12" md="6">
                        <div class="d-flex align-center">
                            <MudIcon Icon="@Icons.Material.Filled.VerifiedUser" Color="Color.Warning" Class="mr-3" />
                            <MudSelect T="int" Label="Senior Supervisor (SSV) para revisiones semanales" Variant="Variant.Outlined" Margin="Margin.Dense">
                                <MudSelectItem Value="0" Disabled>Select SSV...</MudSelectItem>
                                <MudSelectItem Value="1">SSV - Carlos M.</MudSelectItem>
                                <MudSelectItem Value="2">SSV - Roberto G.</MudSelectItem>
                            </MudSelect>
                        </div>
                    </MudItem>
                </MudGrid>
            </MudPaper>
        </MudItem>

        <MudItem xs="12">
            <MudSpacer />
            <div class="d-flex justify-end gap-2 mt-2">
                <MudButton Variant="Variant.Text" Color="Color.Error">Cancelar</MudButton>
                <MudButton OnClick="SaveDocument" StartIcon="@Icons.Material.Filled.Save" Variant="Variant.Filled" Color="Color.Primary">
                    Crear Documento
                </MudButton>
            </div>
        </MudItem>

    </MudGrid>
</MudContainer>

<style>
    .bg-light-grey {
        background-color: var(--mud-palette-background-grey);
    }
    .mud-table-cell {
        vertical-align: middle !important;
        padding-top: 8px !important;
        padding-bottom: 8px !important;
    }
    
    .custom-scrollbar::-webkit-scrollbar {
        height: 8px;
    }
    .custom-scrollbar::-webkit-scrollbar-track {
        background: var(--mud-palette-background-grey); 
        border-radius: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb {
        background: var(--mud-palette-lines-default); 
        border-radius: 4px;
    }
    .custom-scrollbar::-webkit-scrollbar-thumb:hover {
        background: var(--mud-palette-text-secondary); 
    }
</style>

@code {
    private int NumeroCiclos { get; set; } = 3; 

    private Dictionary<string, (string ContentType, string Uri)> UploadedImages { get; set; } = new();
    private List<CreateHRImageDto> ImagesToSave { get; set; } = new();
    private bool disableCreateButton = false;

    private async void UploadFiles(InputFileChangeEventArgs e)
    {
        foreach (var file in e.GetMultipleFiles())
        {
            if (!file.ContentType.StartsWith("image/"))
                continue;

            if (!UploadedImages.ContainsKey(file.Name))
            {
                using (Stream mediaStream = file.OpenReadStream(file.Size))
                {
                    using MemoryStream ms = new();
                    await mediaStream.CopyToAsync(ms);
                    string base64String = Convert.ToBase64String(ms.ToArray());
                    string fullUri = $"data:{file.ContentType};base64,{base64String}";

                    UploadedImages.Add(file.Name, (file.ContentType, fullUri));
                }
            }

            var result = await HRIService.SaveImageInTempFolderAsync(file);
            if (!string.IsNullOrEmpty(result))
            {
                ImagesToSave.Add(new CreateHRImageDto
                {
                    HriId = 0,
                    ImageUrl = result,
                    ImageType = file.ContentType
                });
            }
            
        }

        StateHasChanged();
    }

    private void RemoveImage(string fileName)
    {
        if (UploadedImages.ContainsKey(fileName))
        {
            UploadedImages.Remove(fileName);
        }
    }

    private async void SaveDocument()
    {
        disableCreateButton = true;
        StateHasChanged();


        // Validate Form


        // Save Document Data 
        var dto = new CreateHRIDto
        {
            HRILinesId = null,
            Line = null,
            HRIItemId = null,
            NameOfItem = null,
            ControlNumber = null,
            HRIDockId = null,
            Dock = null,
            Department = null,
            Images = ImagesToSave,
            ItemsRevised = null,
            WeeklyRevisions = null,
            HriCycles = null,
            IsActive = false,
            CreationDate = DateTime.Now,
            HourmeterRevision = null,
            SupervisorId = null
        };
        var response = HRIService.CreateHRI(dto);
    }
}