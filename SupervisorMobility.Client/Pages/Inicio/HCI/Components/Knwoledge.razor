
<MudExpansionPanel Style="min-width: 100%;" IsInitiallyExpanded=true>
    <TitleContent>
        <div Style="align-items: center; display: flex;">
            <MudText>@Title</MudText>
        </div>
    </TitleContent>
    <ChildContent>
        <MudSimpleTable Elevation="0">
            <thead>
                <tr>
                    <th style="text-align:center">Time period</th>
                    <th style="text-align:center">Topic</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in KnowledgeTable)
                {
                    <tr>
                        <td style="width:30%">
                            <MudDateRangePicker @bind-DateRange="item.Value.period" Clearable="true"/>
                        </td>
                        <td>
                            <MudTextField @bind-Value="item.Value.topic" Variant="Variant.Text" FullWidth></MudTextField>
                        </td>
                    </tr>
                }
            </tbody>
        </MudSimpleTable>
    </ChildContent>
</MudExpansionPanel>

