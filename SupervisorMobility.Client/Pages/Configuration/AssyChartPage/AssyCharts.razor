@page "/assychart"
@inject NavigationManager NavigationManager
@inject IJSRuntime JSRuntime
@inject IChecklistService ChecklistService
@using System.Net.Http.Json
@using MudBlazor
@inject ISnackbar Snackbar


@*Title*@
<PageTitle>Supervisor Mobility | Assy Chart</PageTitle>
@*Breadcrumb links*@
<MudBreadcrumbs Class="mb-10" Items="_links">
    <ItemTemplate Context="item">
        <MudButton Href="@item.Href" Variant="Variant.Text" Color="Color.Info" Disabled="item.Disabled">@item.Text</MudButton>
    </ItemTemplate>
</MudBreadcrumbs>

<MudTable Items="_assychart" T="AssyChart" Dense="@dense" Hover="@hover" ReadOnly="@ronly"  
          SortLabel="Sort By">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Assy Chart</MudText>
        
        <MudSpacer />
        <MudTextField @bind-Value="searchString" Placeholder="Search" Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search" IconSize="Size.Medium" Class="mt-0"></MudTextField>
        <MudSpacer />
        <MudIconButton Href="/assychart/createassychart" Icon="@Icons.Material.Filled.Add" aria-label="add"></MudIconButton>

    </ToolBarContent>
    <HeaderContent>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.Stage)">Etapa</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.Distribution)">Distribución</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.GOS)">GOS</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.OperationName)">Nombre de la operación</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.Model)">Model</MudTableSortLabel></MudTh>
        <MudTh><MudTableSortLabel SortBy="new Func<AssyChart, object>(x=>x.Model)">Model</MudTableSortLabel></MudTh>
    </HeaderContent>

    <RowTemplate>
        <MudTd DataLabel="CategoryId">
            <MudText Align="Align.Center">@context.Stage</MudText>
        </MudTd>
        <MudTd DataLabel="Code">
            <MudText Align="Align.Center">@context.Distribution</MudText>
        </MudTd>
        <MudTd DataLabel="Description">
            <MudText Align="Align.Center">@context.GOS</MudText>
        </MudTd>
        <MudTd DataLabel="Sequence">
            <MudText Align="Align.Center">@context.OperationName</MudText>
        </MudTd>
        <MudTd DataLabel="Sequence">
            <MudText Align="Align.Center">@context.Model</MudText>
        </MudTd>
        <MudTd DataLabel="Sequence">
            <MudText Align="Align.Center">@context.Stage</MudText>
        </MudTd>
        <MudTd> 
            <MudText Align="Align.Center">
                <MudIconButton Icon="@Icons.Material.Filled.Info" aria-label="info"></MudIconButton>
                <MudIconButton Icon="@Icons.Material.Filled.Edit" aria-label="edit"></MudIconButton>
                <MudIconButton Icon="@Icons.Material.Filled.Delete" aria-label="Delete"></MudIconButton>
            </MudText>
        </MudTd>
    </RowTemplate>
</MudTable>


