@page "/assychart/BulkAssyCharts"
@using SupervisorMobility.Client.Pages.Configuration.AssyChartPage
@inject IFileUploadAndDownloadService FileUpDoServices
@inject IPlantService PlantServices
@inject IJSRuntime JS
@inject IAssyChartService AssyChartServices
@inject IStringLocalizer<Resource1> Localizer

@*Title*@
<PageTitle>Supervisor Mobility | @Localizer["ACBulkAC"]</PageTitle>
@*Breadcrumb links*@
<MudBreadcrumbs Class="mb-10" Items="_links">
    <ItemTemplate Context="item">
        <MudButton Href="@item.Href" Variant="Variant.Text" Color="Color.Info" Disabled="item.Disabled">@item.Text</MudButton>
    </ItemTemplate>
</MudBreadcrumbs>

<MudSpacer></MudSpacer>
<MudGrid id="Bulk">
    <MudItem xl="10" xs="10">
        <MudItem class="align-center justify-center mud-width-full">
            <MudText Typo="Typo.h6">@Localizer["ACBulkAC"]</MudText>
        </MudItem>
        <MudItem class="d-flex">
            <MudSwitch class="flex-auto" @bind-Checked="@AllPlantsSwitch" Label="@Localizer["ACDownloadAll"]" Color="Color.Info" />
            <MudSpacer />
            @if (!AllPlantsSwitch)
            {
                <MudSelect T="int" Label="@Localizer["ACLabelPlant"]" Variant="Variant.Text" Placeholder="@Localizer["ACSelectPlantBulk"]" Class="mt-3" AnchorOrigin="Origin.BottomCenter" @bind-Value="@plantId" For="@(() => plantId)">
                    <MudSelectItem Disabled="true" Value="0">@Localizer["ACSelectPlantBulk"]</MudSelectItem>
                    @foreach (var type in _plants)
                    {
                        <MudSelectItem Value="type.PlantId">@($"{type.Code} - {type.Description}")</MudSelectItem>
                    }
                </MudSelect>
            }
        </MudItem>
        <MudItem class="my-2">
            <MudButton OnClick="DoBulk" Variant="Variant.Filled" Color="Color.Primary" FullWidth="true">@Localizer["ACDownload"]</MudButton>
        </MudItem>
    </MudItem>
    <MudItem xl="2" xs="2" Class="align-center justify-center">
        <MudImage Src="Images/bulk.png" Alt="Bulk image" Height="120" Width="120" Class="align-center justify-center"> </MudImage>
    </MudItem>
</MudGrid>


