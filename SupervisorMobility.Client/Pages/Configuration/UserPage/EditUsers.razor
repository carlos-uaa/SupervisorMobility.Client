@page "/usersmanagement/EditUser/{userId:int}"
@using SupervisorMobility.Client.Pages.Configuration.AssyChartPage
@inject IPlantService PlantServices
@inject IAreaService AreaServices
@inject IUserService UsersServices
@inject IGroupService GroupServices
@inject NavigationManager NavigationManager
@inject IJSRuntime JS


@*Title*@
<PageTitle>Supervisor Mobility | Detail Users</PageTitle>
@*Breadcrumb links*@
<MudBreadcrumbs Class="mb-10" Items="_links">
    <ItemTemplate Context="item">
        <MudButton Href="@item.Href" Variant="Variant.Text" Color="Color.Info" Disabled="item.Disabled">@item.Text</MudButton>
    </ItemTemplate>
</MudBreadcrumbs>



@if (showui)
{
    <EditForm Model="_user" OnValidSubmit="UpdateUserAsync">
        <DataAnnotationsValidator />
        <MudGrid>
            @*User info*@
            <MudItem xs="12" sm="6" md="3">
                <MudCard>
                    <MudCardContent Style="justify-content:center">
                        <MudText Align="Align.Center" Typo="Typo.h3"><MudIcon Icon="@Icons.Material.Filled.Person"></MudIcon></MudText>
                        <MudItem Class="d-flex d-flex-column justify-center pa-2">
                            <MudText Typo="Typo.h5" Align="Align.Center" Class="pa-2">@_user.Name</MudText>
                        </MudItem>
                        @if (_user.IsActive == true)
                        {
                            <MudText Align="Align.Center" Class="pt-2">
                                <MudChip Variant="Variant.Text" Color="Color.Success">Active</MudChip>
                            </MudText>
                        }
                        else
                        {
                            <MudText Align="Align.Center" Class="pa-4">
                                <MudChip Variant="Variant.Text" Color="Color.Error">Inactive</MudChip>
                            </MudText>
                        }
                    </MudCardContent>
                </MudCard>
            </MudItem>
            @*User details*@
            <MudItem xs="12" sm="6" md="9">
                <MudGrid>
                    <MudItem xs="12">
                        <MudCard>
                            <MudCardHeader>
                               <CardHeaderContent>
                                    <MudText Typo="Typo.h4" Style="text-align:center" Class="mt-5 pl-16 ml-9">Update User</MudText>
                                </CardHeaderContent>
                                <CardHeaderActions>
                                    <MudChip Variant="Variant.Text" Color="Color.Info" Class="mr-15 mt-5">User ID @_user.UserId</MudChip>
                                </CardHeaderActions>
                            </MudCardHeader>
                            <MudCardContent Class="pl-15 pr-15">
                                <MudItem>
                                    <MudItem Class="d-flex flex-column justify-center align-self-center">
                                        <MudTextField Label="User name" @bind-Value="@_user.ObjectId" />
                                        <MudTextField Label="Name" @bind-Value="@_user.Name" />
                                    </MudItem>
                                </MudItem>

                                <MudItem>
                                    <MudItem>
                                        <MudText Typo="Typo.h6" Class="mt-5">Permissions</MudText>
                                    </MudItem>
                                    <MudItem class="d-flex d-flex-column justify-center">
                                        @switch (_user.UserType)
                                        {
                                            case 1:
                                                <MudPaper Elevation="0">
                                                    <MudChip Icon="@Icons.Material.Filled.AdminPanelSettings" Color="Color.Primary">Admin</MudChip>
                                                </MudPaper>
                                                break;
                                            case 2:
                                                <MudPaper Elevation="0">
                                                    <MudChip Icon="@Icons.Material.Filled.SupervisorAccount" Color="Color.Secondary">Senior Supervisor</MudChip>
                                                </MudPaper>
                                                break;
                                            case 3:
                                                <MudPaper Elevation="0">
                                                    <MudChip Icon="@Icons.Material.Filled.SupervisorAccount" Color="Color.Secondary">Supervisor</MudChip>
                                                </MudPaper>
                                                break;
                                            case 4:
                                                <MudPaper Elevation="0">
                                                    <MudChip Icon="@Icons.Material.Filled.PersonOutline" Color="Color.Dark">Operator</MudChip>
                                                </MudPaper>
                                                break;
                                        }
                                    </MudItem>
                                   
                                    <MudItem Class="d-flex flex-row align-center justify-center pa-2 ma-2">
                                        <MudSwitch Class="ma-1" Label="Admin" @bind-Checked="@IsAdmin" @onclick="@(() => setActiveUser(1))" Color="Color.Primary" UnCheckedColor="Color.Dark" />
                                        <MudSwitch Class="ma-1" Label="Senior Supervisor" @bind-Checked="@IsSeniorSupervisor" @onclick="@(() => setActiveUser(2))" Color="Color.Secondary" UnCheckedColor="Color.Dark" />
                                        <MudSwitch Class="ma-1" Label="Supervisor" @bind-Checked="@IsSupervisor" @onclick="@(() => setActiveUser(3))" Color="Color.Secondary" UnCheckedColor="Color.Dark" />
                                        <MudSwitch Class="ma-1" Label="Operator" @bind-Checked="@IsOperator" @onclick="@(() => setActiveUser(4))" Color="Color.Info" UnCheckedColor="Color.Dark" />
                                    </MudItem>
                                </MudItem>



                                <MudItem>
                                    <MudItem>
                                        <MudText Typo="Typo.h6" Class="mt-5">General Info</MudText>
                                    </MudItem>
                                    <MudItem class="d-flex flex-column justify-center align-self-center">

@*                                        @*Mud select of Plants 
                                        <MudSelect T="int" Label="Plant" Variant="Variant.Text" Placeholder="Select a Plant" Class="mt-3" AnchorOrigin="Origin.BottomCenter" @bind-Value="@auxPlant" For="@(() => auxPlant)" SelectedValuesChanged="ShowAreasInvokeByPlantSelector">
                                            <MudSelectItem Disabled="true" Value="0">Select a Plant</MudSelectItem>
                                            @foreach (var type in _plants)
                                            {
                                                <MudSelectItem Value="type.PlantId">@($"{type.Code}-{type.Description}")</MudSelectItem>
                                            }
                                        </MudSelect>*@
@*                                        Mud select of Plants 
                                        <MudSelect T="int" Label="Plant" Variant="Variant.Text" Placeholder="Select a Plant" Class="mt-3" AnchorOrigin="Origin.BottomCenter" @bind-Value="@_user.PlantId" For="@(() => _user.PlantId)" SelectedValuesChanged="ShowAreas">
                                            <MudSelectItem Disabled="true" Value="0">Select a Plant</MudSelectItem>
                                            @foreach (var type in _plants)
                                            {
                                                <MudSelectItem Value="type.PlantId">@($"{type.Code} - {type.Description}")</MudSelectItem>
                                            }
                                        </MudSelect>
                                        Mud select of Areas 
                                        <MudSelect T="int?" Label="Area" Variant="Variant.Text" Placeholder="Select an Area" Class="mt-3" AnchorOrigin="Origin.BottomCenter" @bind-Value="@_user.AreaId" For="@(() => _user.AreaId)">
                                            <MudSelectItem Disabled="true" Value="0">Select an Area</MudSelectItem>
                                            @foreach (var type in _areas)
                                            {
                                                <MudSelectItem Value="type.AreaId">@($"{type.Code} - {type.Description}")</MudSelectItem>
                                            }
                                        </MudSelect>
                                        Mud select of Group 
                                        <MudSelect T="int?" Label="Group" Variant="Variant.Text" Placeholder="Select a Group" Class="mt-3" AnchorOrigin="Origin.BottomCenter" @bind-Value="@_user.GroupId" For="@(() => _user.GroupId)">
                                            <MudSelectItem Disabled="true" Value="0">Select an Group</MudSelectItem>
                                            @foreach (var type in _groups)
                                            {
                                                <MudSelectItem Value="type.GroupId">@($"{type.Code} - {type.Description}")</MudSelectItem>
                                            }
                                        </MudSelect>
*@

                                            @if (_user.UserType != 4)
                                            {
                                                <MudItem>
                                                    <MudTextField Label="Email" @bind-Value="@_user.Email" />
                                                </MudItem>
                                            }
                                            @if (_user.UserType == 4)
                                            {
                                                <MudItem>
                                                    <MudTextField Label="Payroll" @bind-Value="@_user.Payroll" />
                                                </MudItem>
                                            }
                                            @*
                                            <MudTextField Label="Plant" ReadOnly=true @bind-Value="@_user.Plant.Description" />
                                            @if (_user.UserType == 2)
                                            {
                                                @if (_user.Areas?.Count > 0)
                                                {
                                                    <MudText Typo="Typo.h6" Class="mt-5">Areas</MudText>
                                                    @foreach (var area in _user.Areas)
                                                    {
                                                        <MudChip Icon="@Icons.Material.Filled.Business" Color="Color.Info" Class="pl-5 pr-5">@area.Code - @area.Description </MudChip>
                                                    }

                                                }
                                                else
                                                {
                                                    <MudAlert Severity="Severity.Info" Class="mt-5">
                                                        This SSV has no areas yet.
                                                    </MudAlert>
                                                }
                                            }
                                            else
                                            {
                                                <MudText Typo="Typo.h6" Class="mt-5">Area</MudText>
                                                <MudChip Icon="@Icons.Material.Filled.Business" Color="Color.Info" Class="pl-5 pr-5">@_user.Area.Code - @_user.Area.Description </MudChip>
                                            }
                                            <MudTextField Label="Group" Class="mt-5" ReadOnly=true @bind-Value="@_user.Group.Description" />
*@

                                    </MudItem>
                                </MudItem>

                                <MudItem>
                                    <MudItem>
                                        <MudText Typo="Typo.h6" Class="mt-5">More Data For Admin</MudText>
                                    </MudItem>
                                    <MudItem class="d-flex flex-column justify-center align-self-center">
                                        <MudTextField Label="Created Date" ReadOnly=true @bind-Value="@_user.CreatedDate" />
                                        <MudTextField Label="Last Modification Date" ReadOnly=true @bind-Value="@_user.LastUpdated" />
                                        <MudTextField Label="Disable User Date" ReadOnly=true @bind-Value="@_user.DisabledDate" />

                                    </MudItem>
                                </MudItem>


                            </MudCardContent>
                            <MudCardActions>
                                <MudButton ButtonType="ButtonType.Submit" Variant="Variant.Filled" Color="Color.Info" Class="ml-auto mb-10">Save</MudButton>
                                <MudButton Href="/usersmanagement" Variant="Variant.Filled" Class="ml-4 mb-10 mr-13">Cancel</MudButton>
                            </MudCardActions>
                        </MudCard>
                    </MudItem>
                </MudGrid>
            </MudItem>
        </MudGrid>

    </EditForm>

}
else
{


    <MudItem Class="d-flex flex-column align-center justify-center pa-3 ma-2">
        <MudItem class="d-flex justify-center align-self-center pa-3 ma-2 ">
            <MudText Typo="Typo.h6" Class="align-item-center center">Loading Form</MudText>
        </MudItem>
        <MudPaper Class="flex d-flex-row pa-3 ma-2" Elevation="0">
            <MudProgressCircular Color="Color.Primary" Style="height:70px;width:70px;" Indeterminate="true" />
        </MudPaper>
    </MudItem>
}




@code {
    [Parameter]
    public int userId { get; set; }



    private List<BreadcrumbItem> _links = new List<BreadcrumbItem>
        {
            new BreadcrumbItem("Home", href: "#"),
            new BreadcrumbItem("Configuration", href: "/configuration"),
            new BreadcrumbItem("Users", href: "/usersmanagement"),
            new BreadcrumbItem("Edit Users", href: "", disabled: true),
        };

    private User _user = new User();
    private bool showui = false;
    private bool _isAnySwitchActive = false;

    private bool IsAdmin = false;
    private bool IsSeniorSupervisor = false;
    private bool IsSupervisor = false;
    private bool IsOperator = false;


    List<Plant> _plants { get; set; } = new();
    List<Area> _areas = new();
    List<Group> _groups { get; set; } = new();

    private int auxPlant = 0;

    protected async override Task OnInitializedAsync()
    {
        _user = await UsersServices.GetUserAndCollection(userId);
        showui = true;

        _plants = await PlantServices.GetPlants();
        _areas = await AreaServices.GetAreas((int)_user.PlantId);
        _groups = await GroupServices.GetGroups();

        IsAdmin = (_user.UserType == 1) ;
        IsSeniorSupervisor = (_user.UserType == 2);
        IsSupervisor = (_user.UserType == 3);
        IsOperator = (_user.UserType == 4);

        auxPlant = (int)_user.PlantId;

    }

    private async void ShowAreas()
    {
        _user.AreaId = 0;
        _areas = await AreaServices.GetAreas((int)_user.PlantId);
    }

    private void setActiveUser(int switchNumber)
    {
        IsAdmin = ((switchNumber == 1) && !IsAdmin) ? true : false;
        IsSeniorSupervisor = (switchNumber == 2) && !IsSeniorSupervisor ? true : false;
        IsSupervisor = ((switchNumber == 3) && !IsSupervisor) ? true : false;
        IsOperator = ((switchNumber == 4) && !IsOperator) ? true : false;

        if (!IsAdmin && !IsSeniorSupervisor && !IsSupervisor && !IsOperator)
            _user.UserType = 0;
        else
            _user.UserType = switchNumber;


        StateHasChanged();
    }

    private async void UpdateUserAsync()
    {
        _user.LastUpdated = DateTime.Now;

        var result = await UsersServices.UpdateUser(userId, _user);

        if (result)
        {
            await JS.InvokeVoidAsync("alert", "Succesful Update!"); // Alert
            NavigationManager.NavigateTo("/usersmanagement");
        }
        else
            await JS.InvokeVoidAsync("alert", "Fallo Actualizacion!"); // Alert


    }

    private async void ShowAreasInvokeByPlantSelector()
    {
    }
}
