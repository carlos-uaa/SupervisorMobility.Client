@* GeneralUserForm.razor - Para tipos 1 (Admin), 6 (LineSupport), 7 (HeadCount), 8 (RTC) *@
@* Este formulario es mínimo - Los campos básicos (Payroll, Email) se manejan en EditUsers.razor *@

@using SupervisorMobility.Client.Data.Entities

@inject IStringLocalizer<Resource1> Localizer

@code {
    [Parameter] public User User { get; set; } = new();
}
